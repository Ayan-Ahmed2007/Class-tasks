#include <stdio.h>
int main ()
{
    int count,as,inc=1,space=4,n=7;
    for(count=1;count<=9;count++)
    {
        for(as=1;as<=n;as++)
        {
            printf("@");
        }
        if (n == 9)
        {
            inc=-1;
        }
        n+=inc;
        if ()
        space--;
     puts("");   
    }
}