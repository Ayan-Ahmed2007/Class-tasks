#include <stdio.h>
int main ()
{
    int count=1,as=1;
    for (count=1;count<=5;count++)
    {
        for(as=1;as<=5;as++)
        {
            printf("*");
        }
        puts("");
     
    }
}